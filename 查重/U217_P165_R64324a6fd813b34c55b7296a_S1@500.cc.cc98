#include <stdio.h>int Recursion(int a){	if (a <= 2)	return 1;		return Recursion(a - 2) + Recursion(a - 3);}int main(){	int a;	    scanf("%d",&a);        printf("%d\n", Recursion(a));        return 0;}#include<stdio.h>intRecursion(inta){	if(a<=2)	return1;		returnRecursion(a-2)+Recursion(a-3);}intmain(){	inta;	scanf("%d",&a);printf("%d\n",Recursion(a));return0;}#include<stdio.h>intRecursion(inta){if(a<=2)return1;returnRecursion(a-2)+Recursion(a-3);}intmain(){inta;scanf("%d",&a);printf("%d\n",Recursion(a));return0;}#include<stdio.h>intRecursion(inta){if(a<=2)return1;returnRecursion(a-2)+Recursion(a-3);}intmain(){inta;scanf("%d",&a);printf("%d\n",Recursion(a));return0;}