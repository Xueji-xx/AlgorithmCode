#include <stdio.h>#include <math.h>char x;int n;int cnt = 0;//计数 void Recursion(int o){				if (o == cnt)	return;			int count = 2 * (o + 1) - 1;		int Space = cnt - o - 1;		n -= count;			while (Space --){		printf(" ");	}		while (count --){		printf("%c", x);	}		puts("");			Recursion(o + 1);	}int main(){	int a = 1;//每层所需的砖数		int y;		scanf("%d %c",&n,&x);		for(int i = 0 ; i + a <=n ; a+=2){				i += a;				cnt ++;				y = n-i;				}		Recursion(0);		printf("%d",y);	}#include<stdio.h>#include<math.h>charx;intn;intcnt=0;//计数voidRecursion(into){				if(o==cnt)	return;			intcount=2*(o+1)-1;		intSpace=cnt-o-1;		n-=count;			while(Space--){		printf("");	}		while(count--){		printf("%c",x);	}		puts("");			Recursion(o+1);	}intmain(){	inta=1;//每层所需的砖数		inty;		scanf("%d%c",&n,&x);		for(inti=0;i+a<=n;a+=2){				i+=a;				cnt++;				y=n-i;				}		Recursion(0);		printf("%d",y);	}#include<stdio.h>#include<math.h>charx;intn;intcnt=0;//计数voidRecursion(into){if(o==cnt)return;intcount=2*(o+1)-1;intSpace=cnt-o-1;n-=count;while(Space--){printf("");}while(count--){printf("%c",x);}puts("");Recursion(o+1);}intmain(){inta=1;//每层所需的砖数inty;scanf("%d%c",&n,&x);for(inti=0;i+a<=n;a+=2){i+=a;cnt++;y=n-i;}Recursion(0);printf("%d",y);}#include<stdio.h>#include<math.h>charx;intn;intcnt=0;//计数voidRecursion(into){if(o==cnt)return;intcount=2*(o+1)-1;intSpace=cnt-o-1;n-=count;while(Space--){printf("");}while(count--){printf("%c",x);}puts("");Recursion(o+1);}intmain(){inta=1;//每层所需的砖数inty;scanf("%d%c",&n,&x);for(inti=0;i+a<=n;a+=2){i+=a;cnt++;y=n-i;}Recursion(0);printf("%d",y);}