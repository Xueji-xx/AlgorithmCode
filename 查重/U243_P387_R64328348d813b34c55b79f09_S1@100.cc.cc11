#include<iostream>#include<cstdio>#include<algorithm>#include<string>using namespace std; string fun(){	string ans = "";	char ch;	while (cin>>ch)	{		if (ch == '\n')			break;		else if (ch == '[')   		{			int num;			cin >> num;			string tmp = fun();			for (int i = 1; i <= num; i++)			{				ans += tmp;			}		}		else if (ch == ']')		{			return ans;		}		else			ans += ch;	}	return ans;} int main(){	cout << fun() << endl;	return 0;}#include<iostream>#include<cstdio>#include<algorithm>#include<string>usingnamespacestd;stringfun(){	stringans="";	charch;	while(cin>>ch)	{		if(ch=='\n')			break;		elseif(ch=='[')		{			intnum;			cin>>num;			stringtmp=fun();			for(inti=1;i<=num;i++)			{				ans+=tmp;			}		}		elseif(ch==']')		{			returnans;		}		else			ans+=ch;	}	returnans;}intmain(){	cout<<fun()<<endl;	return0;}#include<iostream>#include<cstdio>#include<algorithm>#include<string>usingnamespacestd;stringfun(){stringans="";charch;while(cin>>ch){if(ch=='\n')break;elseif(ch=='['){intnum;cin>>num;stringtmp=fun();for(inti=1;i<=num;i++){ans+=tmp;}}elseif(ch==']'){returnans;}elseans+=ch;}returnans;}intmain(){cout<<fun()<<endl;return0;}#include<iostream>#include<cstdio>#include<algorithm>#include<string>usingnamespacestd;stringfun(){stringans="";charch;while(cin>>ch){if(ch=='\n')break;elseif(ch=='['){intnum;cin>>num;stringtmp=fun();for(inti=1;i<=num;i++){ans+=tmp;}}elseif(ch==']'){returnans;}elseans+=ch;}returnans;}intmain(){cout<<fun()<<endl;return0;}