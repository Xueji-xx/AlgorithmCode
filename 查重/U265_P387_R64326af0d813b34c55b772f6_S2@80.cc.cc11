#include <iostream>#include <cstdio>#include <cstring>#include <algorithm>#include <cmath>using namespace std;const int maxn = 2e7 + 7;string s, tmp, ret;int ptp, pos[maxn];int main() {	cin >> s;	for (int i = 0; i < s.length(); i++) {		if (s[i] == '[')			pos[++ptp] = i;	}	for (int i = ptp; i >= 1; i--) {		int p = pos[i], j, en = 2;		int num = 0;		tmp.clear();		ret.clear();		for (j = p + 1; s[j] != ']'; j++) {			if (s[j] >= '0' && s[j] <= '9') {				num = num * 10 + s[j] - '0';			} else {				tmp += s[j];			}			en++;		}		while (num--)			ret += tmp;		s.erase(p, en);		s.insert(p, ret);	}	cout << s << endl;	return 0;}#include<iostream>#include<cstdio>#include<cstring>#include<algorithm>#include<cmath>usingnamespacestd;constintmaxn=2e7+7;strings,tmp,ret;intptp,pos[maxn];intmain(){	cin>>s;	for(inti=0;i<s.length();i++){		if(s[i]=='[')			pos[++ptp]=i;	}	for(inti=ptp;i>=1;i--){		intp=pos[i],j,en=2;		intnum=0;		tmp.clear();		ret.clear();		for(j=p+1;s[j]!=']';j++){			if(s[j]>='0'&&s[j]<='9'){				num=num*10+s[j]-'0';			}else{				tmp+=s[j];			}			en++;		}		while(num--)			ret+=tmp;		s.erase(p,en);		s.insert(p,ret);	}	cout<<s<<endl;	return0;}#include<iostream>#include<cstdio>#include<cstring>#include<algorithm>#include<cmath>usingnamespacestd;constintmaxn=2e7+7;strings,tmp,ret;intptp,pos[maxn];intmain(){cin>>s;for(inti=0;i<s.length();i++){if(s[i]=='[')pos[++ptp]=i;}for(inti=ptp;i>=1;i--){intp=pos[i],j,en=2;intnum=0;tmp.clear();ret.clear();for(j=p+1;s[j]!=']';j++){if(s[j]>='0'&&s[j]<='9'){num=num*10+s[j]-'0';}else{tmp+=s[j];}en++;}while(num--)ret+=tmp;s.erase(p,en);s.insert(p,ret);}cout<<s<<endl;return0;}#include<iostream>#include<cstdio>#include<cstring>#include<algorithm>#include<cmath>usingnamespacestd;constintmaxn=2e7+7;strings,tmp,ret;intptp,pos[maxn];intmain(){cin>>s;for(inti=0;i<s.length();i++){if(s[i]=='[')pos[++ptp]=i;}for(inti=ptp;i>=1;i--){intp=pos[i],j,en=2;intnum=0;tmp.clear();ret.clear();for(j=p+1;s[j]!=']';j++){if(s[j]>='0'&&s[j]<='9'){num=num*10+s[j]-'0';}else{tmp+=s[j];}en++;}while(num--)ret+=tmp;s.erase(p,en);s.insert(p,ret);}cout<<s<<endl;return0;}