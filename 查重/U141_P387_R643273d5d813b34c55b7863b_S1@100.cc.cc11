#include<iostream>#include<string>using namespace std;string fun() {	string s="", x;	char c;	int d;	while (cin >> c) {//不断的读入字符，直到输入结束（按ctrl+z+enter结束输入）		if (c == '[') {//发现压缩区			cin >> d;//录入每层压缩区需要展开的次数			x = fun();//递归录入，进入内层压缩区，x接收上一层返回的拼接好的字符串			while (d--)s += x;//每出一层，便完成重复d次的拼接		}		else if (c == ']')return s;//出口条件，即是遇到了右括号 		else s += c;//如果不是左右括号，那么还是属于x的字符，接入到后面即可。 	}	return s;//返回最终结果}int main(){	cout << fun();	return 0;}#include<iostream>#include<string>usingnamespacestd;stringfun(){	strings="",x;	charc;	intd;	while(cin>>c){//不断的读入字符，直到输入结束（按ctrl+z+enter结束输入）		if(c=='['){//发现压缩区			cin>>d;//录入每层压缩区需要展开的次数			x=fun();//递归录入，进入内层压缩区，x接收上一层返回的拼接好的字符串			while(d--)s+=x;//每出一层，便完成重复d次的拼接		}		elseif(c==']')returns;//出口条件，即是遇到了右括号		elses+=c;//如果不是左右括号，那么还是属于x的字符，接入到后面即可。	}	returns;//返回最终结果}intmain(){	cout<<fun();	return0;}#include<iostream>#include<string>usingnamespacestd;stringfun(){strings="",x;charc;intd;while(cin>>c){//不断的读入字符，直到输入结束（按ctrl+z+enter结束输入）if(c=='['){//发现压缩区cin>>d;//录入每层压缩区需要展开的次数x=fun();//递归录入，进入内层压缩区，x接收上一层返回的拼接好的字符串while(d--)s+=x;//每出一层，便完成重复d次的拼接}elseif(c==']')returns;//出口条件，即是遇到了右括号elses+=c;//如果不是左右括号，那么还是属于x的字符，接入到后面即可。}returns;//返回最终结果}intmain(){cout<<fun();return0;}#include<iostream>#include<string>usingnamespacestd;stringfun(){strings="",x;charc;intd;while(cin>>c){//不断的读入字符，直到输入结束（按ctrl+z+enter结束输入）if(c=='['){//发现压缩区cin>>d;//录入每层压缩区需要展开的次数x=fun();//递归录入，进入内层压缩区，x接收上一层返回的拼接好的字符串while(d--)s+=x;//每出一层，便完成重复d次的拼接}elseif(c==']')returns;//出口条件，即是遇到了右括号elses+=c;//如果不是左右括号，那么还是属于x的字符，接入到后面即可。}returns;//返回最终结果}intmain(){cout<<fun();return0;}