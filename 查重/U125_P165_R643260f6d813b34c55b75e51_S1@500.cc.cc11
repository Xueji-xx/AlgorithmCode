#include <stdio.h>int n;int jump(int u){	if (u <= 2)		return 1;		return jump(u - 2) + jump(u - 3);}int main(void){    scanf("%d",&n);        printf("%d\n", jump(n));        return 0;}#include<stdio.h>intn;intjump(intu){	if(u<=2)		return1;		returnjump(u-2)+jump(u-3);}intmain(void){scanf("%d",&n);printf("%d\n",jump(n));return0;}#include<stdio.h>intn;intjump(intu){if(u<=2)return1;returnjump(u-2)+jump(u-3);}intmain(void){scanf("%d",&n);printf("%d\n",jump(n));return0;}#include<stdio.h>intn;intjump(intu){if(u<=2)return1;returnjump(u-2)+jump(u-3);}intmain(void){scanf("%d",&n);printf("%d\n",jump(n));return0;}