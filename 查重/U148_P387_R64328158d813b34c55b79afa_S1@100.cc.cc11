#include <bits/stdc++.h>using namespace std;string expand(){	string key = "", x;	char c;	int num;		while(cin>>c)	{		if(c == '[')		{			cin>>num;			x = expand();						while(num--)				key += x;		}  		else   			if(c == ']')				return key;			else  				key += c;	}}int main(void){	cout<<expand();	return 0;}#include<bits/stdc++.h>usingnamespacestd;stringexpand(){	stringkey="",x;	charc;	intnum;		while(cin>>c)	{		if(c=='[')		{			cin>>num;			x=expand();						while(num--)				key+=x;		}		else			if(c==']')				returnkey;			else				key+=c;	}}intmain(void){	cout<<expand();	return0;}#include<bits/stdc++.h>usingnamespacestd;stringexpand(){stringkey="",x;charc;intnum;while(cin>>c){if(c=='['){cin>>num;x=expand();while(num--)key+=x;}elseif(c==']')returnkey;elsekey+=c;}}intmain(void){cout<<expand();return0;}#include<bits/stdc++.h>usingnamespacestd;stringexpand(){stringkey="",x;charc;intnum;while(cin>>c){if(c=='['){cin>>num;x=expand();while(num--)key+=x;}elseif(c==']')returnkey;elsekey+=c;}}intmain(void){cout<<expand();return0;}