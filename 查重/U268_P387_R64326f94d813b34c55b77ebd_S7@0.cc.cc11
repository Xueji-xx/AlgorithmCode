#include <stdio.h>void dog() {    char input[1001];    gets(input);    int len = strlen(input);    for (int i = 0; i < len; i++) {        int count = 1;        while (input[i + 1] == input[i]) {            count++;            i++;        }        if (count > 1) {            printf("%d", count);        }        putchar(input[i]);    }}void cat() {    char input[1001];    gets(input);    int len = strlen(input);    for (int i = 0; i < len; i++) {        int count = 0;        while (input[i] >= '0' && input[i] <= '9') {            count = count * 10 + input[i] - '0';            i++;        }        for (int j = 0; j < count; j++) {            putchar(input[i]);        }        if (count == 0) {            putchar(input[i]);        }    }}int main() {    char ch;    scanf("%c", &ch);    getchar();    if (ch == 'C') {        dog();    } else if (ch == 'D') {        cat();    }    return 0;}#include<stdio.h>voiddog(){charinput[1001];gets(input);intlen=strlen(input);for(inti=0;i<len;i++){intcount=1;while(input[i+1]==input[i]){count++;i++;}if(count>1){printf("%d",count);}putchar(input[i]);}}voidcat(){charinput[1001];gets(input);intlen=strlen(input);for(inti=0;i<len;i++){intcount=0;while(input[i]>='0'&&input[i]<='9'){count=count*10+input[i]-'0';i++;}for(intj=0;j<count;j++){putchar(input[i]);}if(count==0){putchar(input[i]);}}}intmain(){charch;scanf("%c",&ch);getchar();if(ch=='C'){dog();}elseif(ch=='D'){cat();}return0;}#include<stdio.h>voiddog(){charinput[1001];gets(input);intlen=strlen(input);for(inti=0;i<len;i++){intcount=1;while(input[i+1]==input[i]){count++;i++;}if(count>1){printf("%d",count);}putchar(input[i]);}}voidcat(){charinput[1001];gets(input);intlen=strlen(input);for(inti=0;i<len;i++){intcount=0;while(input[i]>='0'&&input[i]<='9'){count=count*10+input[i]-'0';i++;}for(intj=0;j<count;j++){putchar(input[i]);}if(count==0){putchar(input[i]);}}}intmain(){charch;scanf("%c",&ch);getchar();if(ch=='C'){dog();}elseif(ch=='D'){cat();}return0;}#include<stdio.h>voiddog(){charinput[1001];gets(input);intlen=strlen(input);for(inti=0;i<len;i++){intcount=1;while(input[i+1]==input[i]){count++;i++;}if(count>1){printf("%d",count);}putchar(input[i]);}}voidcat(){charinput[1001];gets(input);intlen=strlen(input);for(inti=0;i<len;i++){intcount=0;while(input[i]>='0'&&input[i]<='9'){count=count*10+input[i]-'0';i++;}for(intj=0;j<count;j++){putchar(input[i]);}if(count==0){putchar(input[i]);}}}intmain(){charch;scanf("%c",&ch);getchar();if(ch=='C'){dog();}elseif(ch=='D'){cat();}return0;}